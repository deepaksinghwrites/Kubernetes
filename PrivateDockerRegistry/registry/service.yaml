apiVersion: v1
kind: Service
metadata:
  name: private-docker-registry
  namespace: default
spec:
  selector:
    app: private-docker-registry
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP
